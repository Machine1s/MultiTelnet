# Python 缓存
__pycache__/
*.py[cod]
*$py.class

# 打包工具产生的临时文件 (非常大，不要管理)
build/
dist/
release/
*.spec

# 项目生成的审计日志 (属于运行数据，不属于代码)
logs/*.csv

# 虚拟环境
venv/
.env

# 【关键】敏感配置文件
# 不要通过 Git 同步含有真实密码的主机列表
inventory/hosts.yaml

# 操作系统临时文件
.DS_Store
Thumbs.db
